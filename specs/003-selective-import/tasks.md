---
description: "Task list for Selective Import (003) — generated by speckit.tasks"
---

# Tasks: Selective Import (003)

**Feature Branch**: `003-selective-import`
**Spec**: `specs/003-selective-import/spec.md`
**Plan**: `specs/003-selective-import/plan.md`
**Input docs**: spec.md, plan.md, research.md, data-model.md, contracts/api.md, quickstart.md
**Tests**: Required (Constitution Principle IV)

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel with other [P] tasks in the same phase (different files, no blocking dependencies)
- **[US1/US2/US3]**: Traces task to its user story
- Exact file paths are included in every task description

---

## Phase 1: Setup

**Purpose**: Confirm branch and environment are ready before any code changes

- [X] T001 Confirm branch `003-selective-import` is checked out, run `pnpm install` from repo root, and verify local PostgreSQL 16 is running per quickstart.md prerequisites

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Schema, interfaces, and validation primitives that MUST exist before any user story work can begin. All three tasks are independent and can run in parallel.

**⚠️ CRITICAL**: No user story work can begin until this phase is complete.

- [X] T002 Add `importEverything Boolean @default(true)` and `selectedSubSourceIds String[] @default([])` to `Integration` model and run `npx prisma migrate dev --name add_selective_import_to_integration` in `backend/prisma/schema.prisma` + `backend/prisma/migrations/`
- [X] T003 [P] Add `SubSource` interface, optional `listSubSources?(integrationId: string): Promise<SubSource[]>` method to `IntegrationAdapter`, and optional `subSourceId?: string` field to `NormalizedItem` in `backend/src/integrations/_adapter/types.ts`
- [X] T004 [P] Add `importFilterBodySchema` Zod schema (validates `importEverything: boolean` + `selectedSubSourceIds: string[]`, refine: rejects empty array when `importEverything=false`) and `ImportFilterBody` type in `backend/src/api/schemas/integrations.schemas.ts`

**Checkpoint**: Migration applied, interface extended, schema ready — user story phases can begin

---

## Phase 3: User Story 1 — Configure Import Filters During Integration Onboarding (Priority: P1) 🎯 MVP

**Goal**: When a user connects any integration and completes OAuth, they are presented with available sub-sources, can select a subset (or enable "Import everything"), and the first sync respects that selection.

**Independent Test**: Connect any integration in a fresh account → select a subset of sub-sources → trigger a sync → verify only items from selected sub-sources appear in the feed. Verifiable without US2 or US3.

**Acceptance Scenarios covered**: AC1, AC2, AC3, AC5 from spec.md US1

### Tests for User Story 1 ⚠️ Write tests FIRST — all must FAIL before implementation begins

- [X] T005 [P] [US1] Write failing unit tests for Gmail adapter: `sync()` returns filtered items when `importEverything=false` + known `selectedSubSourceIds`; `sync()` returns all items when `importEverything=true`; `sync()` returns empty array when `importEverything=false` + empty `selectedSubSourceIds`; `listSubSources()` returns mapped `SubSource[]` from mocked Gmail Labels API response; `listSubSources()` returns `[]` when provider returns error in `backend/src/integrations/gmail/__tests__/gmail.sync.test.ts`
- [X] T006 [P] [US1] Write failing unit tests for Microsoft Tasks adapter: `sync()` filter behaviour (same pattern as Gmail); `listSubSources()` returns `SubSource[]` mapped from mocked Graph `/me/todo/lists` response in `backend/src/integrations/microsoft-tasks/__tests__/microsoft-tasks.sync.test.ts`
- [X] T007 [P] [US1] Write failing unit tests for Apple Reminders adapter: `sync()` filter behaviour; `listSubSources()` returns `SubSource[]` from mocked CalDAV PROPFIND VTODO collection response in `backend/src/integrations/apple-reminders/__tests__/apple-reminders.sync.test.ts`
- [X] T008 [P] [US1] Write failing unit tests for Apple Calendar adapter: `sync()` filter behaviour; `listSubSources()` returns `SubSource[]` from mocked CalDAV PROPFIND VEVENT collection response in `backend/src/integrations/apple-calendar/__tests__/apple-calendar.sync.test.ts`
- [X] T009 [P] [US1] Write failing API route tests: `GET /api/integrations/:serviceId/sub-sources` returns 200 with `subSources` array; returns 404 when integration not connected; returns 502 when provider API fails; `PUT /api/integrations/:serviceId/import-filter` saves `importEverything + selectedSubSourceIds` and returns 200; returns 400 when `importEverything=false` + empty array; returns 404 when integration not connected; `GET /api/integrations` response includes `importEverything` and `selectedSubSourceIds` fields in `backend/src/api/__tests__/integration-filter.routes.test.ts`
- [X] T010 [P] [US1] Write failing React Testing Library tests for `SubSourceSelector`: renders sub-source list fetched from API; Save button is enabled with ≥1 selection; shows loading state while fetching sub-sources; shows API error state with retry button in `frontend/src/components/integrations/__tests__/SubSourceSelector.test.tsx`

### Implementation for User Story 1

- [X] T011 [P] [US1] Implement `listSubSources()` in Gmail adapter (calls `GET https://gmail.googleapis.com/gmail/v1/users/me/labels`, decrypts stored token, maps `{ id, name }` → `{ id, label, type: 'label' }`); set `subSourceId` on each `NormalizedItem` during item construction; add `importEverything`/`selectedSubSourceIds` filter at end of `sync()` before return in `backend/src/integrations/gmail/index.ts`
- [X] T012 [P] [US1] Implement `listSubSources()` in Microsoft Tasks adapter (calls `GET https://graph.microsoft.com/v1.0/me/todo/lists`, maps `{ id, displayName }` → `{ id, label, type: 'list' }`); set `subSourceId` on each `NormalizedItem`; add filter logic in `sync()` in `backend/src/integrations/microsoft-tasks/index.ts`
- [X] T013 [P] [US1] Implement `listSubSources()` in Apple Reminders adapter (CalDAV PROPFIND depth-1 on reminders principal URL, enumerate VTODO collections, map `href` → `id` and `displayname` → `label`, `type: 'list'`); set `subSourceId` on each `NormalizedItem`; add filter logic in `sync()` in `backend/src/integrations/apple-reminders/index.ts`
- [X] T014 [P] [US1] Implement `listSubSources()` in Apple Calendar adapter (CalDAV PROPFIND depth-1 on calendar home set, enumerate VEVENT collections, map `href` → `id` and `displayname` → `label`, `type: 'calendar'`); set `subSourceId` on each `NormalizedItem`; add filter logic in `sync()` in `backend/src/integrations/apple-calendar/index.ts`
- [X] T015 [US1] Extend `IntegrationStatusItem` type to add `importEverything: boolean` and `selectedSubSourceIds: string[]`; update `listIntegrations()` Prisma select clause to include both new fields in `backend/src/integrations/integration.service.ts`
- [X] T016 [US1] Add `getSubSources(userId, serviceId)` function (finds integration, calls `adapter.listSubSources?()`, returns `[]` if adapter does not implement method); add `updateImportFilter(userId, serviceId, importEverything, selectedSubSourceIds)` function (Prisma update with `userId_serviceId` compound key, returns updated `IntegrationStatusItem`) in `backend/src/integrations/integration.service.ts`
- [X] T017 [US1] Add `GET /api/integrations/:serviceId/sub-sources` route: validate `serviceId`, call `getSubSources()`, return `{ subSources }` on 200; map integration-not-found to 404; map provider API errors to 502 with `{ error: 'SubSourceFetchError', message }` in `backend/src/api/integrations.routes.ts`
- [X] T018 [US1] Add `PUT /api/integrations/:serviceId/import-filter` route: validate `serviceId`, parse body with `importFilterBodySchema.safeParse()`, return 400 on validation failure, call `updateImportFilter()`, return 200 with updated filter state including `updatedAt` ISO string in `backend/src/api/integrations.routes.ts`
- [X] T019 [US1] Update `GET /api/integrations` handler to pass through `importEverything` and `selectedSubSourceIds` from `listIntegrations()` result into the response array (fields come through automatically after T015) in `backend/src/api/integrations.routes.ts`
- [X] T020 [P] [US1] Add `SubSource` interface; add `importEverything: boolean` and `selectedSubSourceIds: string[]` to `IntegrationStatus` interface; add `listSubSources(serviceId: ServiceId): Promise<SubSource[]>` function (fetches `GET /api/integrations/:serviceId/sub-sources`, throws on non-OK); add `updateImportFilter(serviceId, filter)` function (PUT with JSON body, throws with message from error response body) in `frontend/src/services/integrations.service.ts`
- [X] T021 [US1] Create `SubSourceSelector` component with props `{ serviceId, importEverything, selectedSubSourceIds, onSave, onCancel? }`: on mount fetch sub-sources via `listSubSources()`; show checklist of fetched sub-sources; "Import everything" toggle at top (wired to `importEverything` prop, disables individual checkboxes when ON); Save button calls `onSave({ importEverything, selectedSubSourceIds })`; loading skeleton while fetching; error state with retry button (fallback: "Import everything" offered per FR-011) in `frontend/src/components/integrations/SubSourceSelector.tsx`
- [X] T022 [US1] Update `IntegrationCard` to accept `importEverything?: boolean` (default `true`) and `selectedSubSourceIds?: string[]` (default `[]`) props; add "Edit import filter" button shown when `status === 'connected'`; on click expand inline panel rendering `<SubSourceSelector />` with current props; on `onSave` call `updateImportFilter()` then invoke parent refresh callback; collapse panel on save or cancel in `frontend/src/components/integrations/IntegrationCard.tsx`
- [X] T023 [US1] Add URL param handling in integrations settings page: after OAuth callback with `?connected={serviceId}`, if `?step=import-filter` is also present automatically open `SubSourceSelector` inline panel for that integration with `importEverything=true` and `selectedSubSourceIds=[]` as initial state in `frontend/src/app/settings/integrations/page.tsx`

**Checkpoint**: US1 fully functional — run `cd backend && pnpm test` to confirm T005–T009 tests pass; manually test connect → filter → sync end-to-end

---

## Phase 4: User Story 2 — Edit Import Filters After Initial Setup (Priority: P2)

**Goal**: A user who has already connected an integration can navigate to settings, open the import filter panel on their IntegrationCard, change the sub-source selection, save, and the next sync reflects the updated selection. Removing a sub-source does not delete previously imported items.

**Independent Test**: Connect with P1 flow → select subset A → sync → verify subset A items appear → navigate to integration settings → change selection to subset B → sync again → verify only subset B items appear. No new OAuth connection needed.

**Acceptance Scenarios covered**: AC1, AC2, AC3, AC4 from spec.md US2

### Tests for User Story 2 ⚠️ Write tests FIRST — must FAIL before implementation

- [X] T024 [P] [US2] Write failing API route test: after `PUT /import-filter` with updated selection, `GET /api/integrations` response shows the new `importEverything` and `selectedSubSourceIds` values in `backend/src/api/__tests__/integration-filter.routes.test.ts`
- [X] T025 [P] [US2] Write failing React Testing Library test: `IntegrationCard` renders edit button for `status='connected'`; clicking it opens `SubSourceSelector` panel pre-populated with `importEverything` and `selectedSubSourceIds` prop values in `frontend/src/components/integrations/__tests__/IntegrationCard.test.tsx`

### Implementation for User Story 2

- [X] T026 [US2] Pass `importEverything` and `selectedSubSourceIds` from `listIntegrations()` response through to each `<IntegrationCard />` on the settings page so the edit panel opens with the user's current persisted selection in `frontend/src/app/settings/integrations/page.tsx`
- [X] T027 [US2] Add zero-selection guard to `SubSourceSelector`: disable Save button and display inline validation message ("Select at least one sub-source, or enable Import everything") when `importEverything=false` and `selectedSubSourceIds` is empty; this mirrors the backend `importFilterBodySchema` validation (FR-010) in `frontend/src/components/integrations/SubSourceSelector.tsx`

**Checkpoint**: US2 fully functional — edit filter in settings, re-sync, verify selection is respected; previously imported items not deleted

---

## Phase 5: User Story 3 — "Import Everything" Quick Toggle Per Integration (Priority: P3)

**Goal**: "Import everything" is ON by default for every new integration. When enabled, the toggle bypasses sub-source selection and imports all items including any new sub-sources added to the user's account after setup. When disabled, individual checkboxes become active.

**Independent Test**: Connect an integration → leave "Import everything" ON (default) → sync → verify all items from integration appear without selecting individual sub-sources.

**Acceptance Scenarios covered**: AC1, AC2, AC3 from spec.md US3

### Tests for User Story 3 ⚠️ Write tests FIRST — must FAIL before implementation

- [X] T028 [P] [US3] Write failing unit test: Gmail `sync()` with `importEverything=true` returns ALL items regardless of `selectedSubSourceIds` content (including an item whose `subSourceId` is NOT in `selectedSubSourceIds`) in `backend/src/integrations/gmail/__tests__/gmail.sync.test.ts`
- [X] T029 [P] [US3] Write failing React Testing Library tests: `SubSourceSelector` with `importEverything=true` renders all checkboxes checked and disabled; toggling "Import everything" OFF enables checkboxes and restores previous `selectedSubSourceIds`; toggling back ON disables checkboxes without clearing selection in `frontend/src/components/integrations/__tests__/SubSourceSelector.test.tsx`

### Implementation for User Story 3

- [X] T030 [US3] Implement "Import everything" toggle state management in `SubSourceSelector`: when toggle switches ON, mark all checkboxes as checked+disabled (visual only — actual filter bypassed server-side); when toggle switches OFF, re-enable checkboxes with prior `selectedSubSourceIds` restored; local state tracks `localImportEverything` and `localSelectedIds` separately so toggling is reversible per INV-003 in `frontend/src/components/integrations/SubSourceSelector.tsx`
- [X] T031 [US3] Verify in onboarding integration page that new integrations render `SubSourceSelector` with `importEverything={true}` by default (sourced from migration default); add a Vitest assertion confirming `importEverything` defaults to `true` in a freshly seeded `Integration` row in `backend/src/api/__tests__/integration-filter.routes.test.ts`

**Checkpoint**: All three user stories independently functional — run full test suite: `cd backend && pnpm test && cd ../frontend && pnpm test`

---

## Phase 6: Polish & Cross-Cutting Concerns

**Purpose**: Edge case handling, UX refinements, and final validation across all user stories

- [X] T032 [P] Add stale sub-source detection to `SubSourceSelector`: cross-reference `selectedSubSourceIds` against fetched `subSources` list; render an `[unavailable]` badge (greyed, checked but disabled) for any stored ID not returned by `listSubSources()` — visible so user knows to deselect; stale IDs remain in `selectedSubSourceIds` until user explicitly removes them per FR-013 in `frontend/src/components/integrations/SubSourceSelector.tsx`
- [X] T033 [P] Add text search input to `SubSourceSelector` (rendered only when `subSources.length > 10`): filters visible checklist items in real time by `label` field (case-insensitive); search does not affect `selectedSubSourceIds` state — items filtered out of view but already checked remain selected per FR-012 in `frontend/src/components/integrations/SubSourceSelector.tsx`
- [X] T034 [P] Implement provider-fetch error fallback in `SubSourceSelector` (FR-011): on `listSubSources()` 502/network error, show error message + "Retry" button + "Can't load sub-sources — Import everything will be used" copy; force `importEverything=true` in the error state so onboarding can still complete; retry re-calls `listSubSources()` in `frontend/src/components/integrations/SubSourceSelector.tsx`
- [X] T035 [P] Add React Testing Library tests for polish behaviours: `[unavailable]` badge renders for stale IDs; search input appears for 11+ sub-sources and filters the list; error fallback renders with retry button; retry re-fetches and shows list in `frontend/src/components/integrations/__tests__/SubSourceSelector.test.tsx`
- [X] T036 Run `cd backend && pnpm test` — confirm all 14 backend test cases from quickstart.md §Step 11 pass (zero failures)
- [X] T037 Run `cd frontend && pnpm test` — confirm all 5 frontend `SubSourceSelector` test cases from quickstart.md §Step 11 pass (zero failures)
- [X] T038 Execute `specs/003-selective-import/quickstart.md` verification checklist end-to-end: migration, existing sync unchanged, all API routes, sync filter SC-002, "Import everything" default SC-004, zero-selection rejection FR-010, scrollable+searchable UI FR-012

---

## Dependencies & Execution Order

### Phase Dependencies

```
Phase 1 (Setup)       → no dependencies; start immediately
Phase 2 (Foundational) → depends on Phase 1; BLOCKS all user stories
Phase 3 (US1 P1)      → depends on Phase 2 complete; no other story deps
Phase 4 (US2 P2)      → depends on Phase 2 complete; integrates with US1 components (built in Phase 3)
Phase 5 (US3 P3)      → depends on Phase 2 complete; extends Phase 3 components
Phase 6 (Polish)      → depends on Phases 3–5 complete
```

### User Story Dependencies

| Story | Can Start After | Depends On Other Stories? |
|-------|----------------|--------------------------|
| **US1 (P1)** | Phase 2 complete | None — independently testable |
| **US2 (P2)** | Phase 2 complete | Uses components from US1, but US1 must be complete first |
| **US3 (P3)** | Phase 2 complete | Uses components from US1, but can extend independently |

### Within Each User Story

1. Tests FIRST (write all test tasks, confirm they fail)
2. Adapter implementations (T011–T014) before service layer (T015–T016)
3. Service layer (T015–T016) before API routes (T017–T019)
4. API routes (T017–T019) before frontend service (T020)
5. Frontend service (T020) before components (T021–T023)

### Parallel Opportunities

**Phase 2** — all three tasks independent (different files):
- T002 (migration) ‖ T003 (types.ts) ‖ T004 (schemas.ts)

**Phase 3 Tests** — all six test-writing tasks are independent:
- T005 ‖ T006 ‖ T007 ‖ T008 ‖ T009 ‖ T010

**Phase 3 Adapter Implementations** — all four adapters independent:
- T011 (Gmail) ‖ T012 (MS Tasks) ‖ T013 (Apple Reminders) ‖ T014 (Apple Calendar)

**Phase 3 Frontend** — service update is independent of component work:
- T020 (frontend service) ‖ T021 (SubSourceSelector) [once T020 is done, T022→T023 are sequential]

**Phase 6 Polish** — all independent (same file but non-overlapping sections; or can be split across branches):
- T032 ‖ T033 ‖ T034 ‖ T035

---

## Parallel Execution Examples

### Phase 2: Foundational (3-way parallel)

```bash
# Three independent files — all can be written simultaneously:
Task: "T002 — DB migration in backend/prisma/schema.prisma"
Task: "T003 — Interface extensions in backend/src/integrations/_adapter/types.ts"
Task: "T004 — Zod schema in backend/src/api/schemas/integrations.schemas.ts"
```

### Phase 3: US1 Tests (6-way parallel)

```bash
Task: "T005 — Gmail adapter unit tests"
Task: "T006 — Microsoft Tasks adapter unit tests"
Task: "T007 — Apple Reminders adapter unit tests"
Task: "T008 — Apple Calendar adapter unit tests"
Task: "T009 — API route tests"
Task: "T010 — SubSourceSelector component tests"
```

### Phase 3: US1 Adapter Implementations (4-way parallel)

```bash
Task: "T011 — Gmail: listSubSources() + subSourceId + filter in sync()"
Task: "T012 — Microsoft Tasks: listSubSources() + subSourceId + filter in sync()"
Task: "T013 — Apple Reminders: listSubSources() + subSourceId + filter in sync()"
Task: "T014 — Apple Calendar: listSubSources() + subSourceId + filter in sync()"
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Phase 1: Setup (T001)
2. Complete Phase 2: Foundational (T002–T004) — **REQUIRED, blocks everything**
3. Write Phase 3 tests first (T005–T010) — confirm they fail
4. Implement Phase 3 (T011–T023)
5. **STOP and VALIDATE**: run `pnpm test`, manually test onboarding filter flow end-to-end
6. Deploy/demo — this is the MVP: users can configure filters at onboarding

### Incremental Delivery

```
MVP:  Setup + Foundational + US1  →  import filters during onboarding
+P2:  + US2                        →  edit filters in settings
+P3:  + US3                        →  polished toggle UX + default-on behavior
+∞:   + Polish phase               →  stale handling, search, error fallback
```

### Parallel Team Strategy

With ≥2 developers after Phase 2 completes:
- **Dev A**: T011+T012 (Gmail + MS Tasks adapters) → T015 → T016 → T017 → T018 → T019
- **Dev B**: T013+T014 (Apple adapters) → T020 → T021 → T022 → T023

With ≥3 developers:
- **Dev A**: T011+T012 → service layer (T015–T016)
- **Dev B**: T013+T014 → API routes (T017–T019)
- **Dev C**: T020 → T021 → T022 → T023 (frontend)

---

## Notes

- All `[P]` tasks operate on different files — no merge conflicts within a phase
- `[US1/US2/US3]` labels trace each task to its user story for traceability
- Tests MUST be written first and MUST fail before the corresponding implementation task begins
- `importEverything=true` default (T002 migration) preserves existing behaviour for all current users — zero breaking change (SC-004)
- Sub-sources are never persisted — only stable external IDs are stored; `listSubSources()` is always a live call
- Filter logic stays exclusively inside each adapter's `sync()` — no worker/core code change needed (Constitution Principle I)
- The `selectedSubSourceIds` field is retained when `importEverything` is toggled back to `true` (INV-003) — do not clear it on toggle
- Commit after each task or logical group; each phase checkpoint is a safe merge point
