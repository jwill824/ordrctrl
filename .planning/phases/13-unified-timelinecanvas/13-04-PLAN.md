---
phase: 13-unified-timelinecanvas
plan: 04
type: execute
wave: 4
depends_on:
  - 13-03-PLAN.md
files_modified:
  - frontend/tests/e2e/weekly-navigation.spec.ts
autonomous: false
requirements:
  - CANVAS-01
  - CANVAS-02
  - CANVAS-03
  - CANVAS-04
  - CANVAS-05
must_haves:
  truths:
    - "weekly-navigation.spec.ts e2e tests still pass with the new TimelineCanvas structure"
    - "Human has visually verified all 5 CANVAS success criteria in a browser"
  artifacts:
    - path: "frontend/tests/e2e/weekly-navigation.spec.ts"
      provides: "Updated e2e tests using TimelineCanvas week mode selectors"
      contains: "TimelineCanvas"
  key_links: []
---

<objective>
Verify all 5 CANVAS success criteria through e2e test updates and human visual sign-off. The weekly-navigation e2e tests relied on `WeeklyPlannerView`'s DOM structure; update them for `TimelineCanvas` if any selectors broke. Then confirm the full experience in a browser.

This plan is marked `autonomous: false` — the executor must pause for human visual review after running the dev server.
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-unified-timelinecanvas/13-CONTEXT.md
@.planning/ROADMAP.md
@frontend/tests/e2e/weekly-navigation.spec.ts
@frontend/tests/unit/components/TimelineCanvas.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review and update weekly-navigation e2e test selectors</name>
  <files>frontend/tests/e2e/weekly-navigation.spec.ts</files>
  <action>
    Review `frontend/tests/e2e/weekly-navigation.spec.ts`. The tests rely on these selectors:
    - `page.locator('button:has-text("Week")')` — the Day/Week toggle button (still in `feed/page.tsx` header, unchanged)
    - `page.locator('[aria-label="Previous week"]')` — was in `WeeklyPlannerView`, now in `WeekNavHeader` component inside `feed/page.tsx`
    - `page.locator('[aria-label="Next week"]')` — same
    - `page.locator('button:has-text("Today")')` — same
    - `page.locator('button:has-text("${expectedRange}")')` — date range label in `WeekNavHeader`

    All aria-label selectors are preserved in `WeekNavHeader` (identical to the old `WeeklyPlannerView`). The tests should pass without changes.

    However, verify that `WeekNavHeader` in `feed/page.tsx` uses these exact aria-labels:
    - `aria-label="Previous week"` on the prev button ✓
    - `aria-label="Next week"` on the next button ✓

    If they match, no changes to the e2e file are needed. If any selector differs, update the e2e test to match the new DOM.

    Also add a new test for CANVAS-03 (column headers visible in week mode):

    ```ts
    test('TC-WN-05: week mode shows day column headers (Mon–Sun)', async ({ page }) => {
      await switchToWeekView(page);
      // Each day column header contains a day abbreviation
      // Check at least Mon and Fri are visible
      await expect(page.locator('text=/Mon \\d+/')).toBeVisible();
      await expect(page.locator('text=/Fri \\d+/')).toBeVisible();
    });
    ```

    Add TC-WN-05 inside the `test.describe('Weekly view navigation — authenticated', ...)` block.
  </action>
  <verify>
    <automated>cd frontend && grep "TC-WN-05" tests/e2e/weekly-navigation.spec.ts</automated>
  </verify>
  <done>
    `TC-WN-05` exists in `weekly-navigation.spec.ts`. File has no broken imports.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run unit tests one final time</name>
  <files></files>
  <action>
    Run the full frontend unit test suite as a final health check:

    ```bash
    cd frontend && pnpm vitest run
    ```

    All tests must pass. Zero failures acceptable.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>
    All unit tests pass. Output shows 0 failed.
  </done>
</task>

<task type="manual">
  <name>Task 3: Human visual verification — all 5 CANVAS success criteria</name>
  <files></files>
  <action>
    Start the dev server (`pnpm dev` from repo root or `cd frontend && pnpm dev`) and open http://localhost:3000/feed.

    Verify each success criterion:

    **CANVAS-01** — Single component, columns prop:
    - [ ] Toggle between Day and Week mode using the header toggle
    - [ ] Confirm the same canvas area updates (no flash/remount), just column count changes

    **CANVAS-02** — Shared time axis always on left:
    - [ ] In Day mode: hour labels (12am…11pm) appear in a column on the left side
    - [ ] In Week mode: hour labels still appear on the left (sparse: 12a, 6a, 12p, 6p)

    **CANVAS-03** — Week column headers + today highlight:
    - [ ] Each of 7 columns shows DAY_ABBR + date (e.g., "Mon 9", "Fri 13")
    - [ ] Today's column has a subtle grey background tint (`bg-zinc-50`)
    - [ ] Today's column header has a black top border and bold text

    **CANVAS-04** — Block degradation in narrow week columns:
    - [ ] Create a 20–25 minute task and view it in week mode — title truncates, no time label
    - [ ] Same task in day mode — time label visible

    **CANVAS-05** — 200ms toggle animation:
    - [ ] Toggle Day → Week: columns expand with a smooth ~200ms transition
    - [ ] Toggle Week → Day: columns collapse with a smooth ~200ms transition

    **Regression checks:**
    - [ ] Drag-to-reschedule still works in Day mode
    - [ ] Drag-to-reschedule works in Week mode (wider blocks)
    - [ ] Tap-to-edit opens TaskSheet in Day mode
    - [ ] Week nav prev/next/Today controls work correctly
    - [ ] Source filter pills work in both modes
    - [ ] All-day banner appears in Day mode when all-day items present
    - [ ] Unscheduled section appears below the canvas in Day mode

    Record any issues found. If a criterion fails, fix it and re-verify before marking done.
  </action>
  <verify>
    <human>All 5 CANVAS success criteria verified visually. All regression checks pass.</human>
  </verify>
  <done>
    Human confirms all 5 CANVAS criteria pass and no regressions observed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update ROADMAP.md with phase 13 plans</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    In `.planning/ROADMAP.md`, update the Phase 13 entry to reflect the 4 plans:

    Find:
    ```
    **Plans**: TBD
    ```
    (under Phase 13)

    Replace with:
    ```
    **Plans**: 4 plans
    Plans:
    - [ ] 13-01-PLAN.md — Write RED tests for TimelineCanvas (unit tests covering CANVAS-01 through CANVAS-04)
    - [ ] 13-02-PLAN.md — Build TimelineCanvas component, update constants/thresholds (GREEN)
    - [ ] 13-03-PLAN.md — Wire feed/page.tsx, structural flex conversion, delete old views
    - [ ] 13-04-PLAN.md — E2e test update + human verify all 5 CANVAS success criteria
    ```

    Also update the progress table row for Phase 13:

    Find:
    ```
    | 13: Unified TimelineCanvas | v1.1 | 0/TBD | Not started | — |
    ```

    Replace with:
    ```
    | 13: Unified TimelineCanvas | v1.1 | 0/4 | Not started | — |
    ```
  </action>
  <verify>
    <automated>grep "13-04-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    ROADMAP.md reflects 4 plans for Phase 13.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e tests → browser | Tests run against local dev server; no production exposure |
| human verification | Final sign-off by developer; no automated trust required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-13-04-E2E | Tampering | e2e selectors | accept | Tests use aria-labels; selectors are stable across refactors |
</threat_model>

<verification>
1. `cd frontend && pnpm vitest run` — all unit tests pass
2. `frontend/tests/e2e/weekly-navigation.spec.ts` has TC-WN-05 (column header test)
3. `.planning/ROADMAP.md` shows `0/4` plans for Phase 13 with all 4 plan entries listed
4. Human has signed off on all 5 CANVAS success criteria
</verification>

<success_criteria>
- All 5 CANVAS requirements verified: CANVAS-01 (single component), CANVAS-02 (shared axis), CANVAS-03 (column headers + today), CANVAS-04 (28px degradation), CANVAS-05 (200ms animation)
- Weekly navigation e2e tests updated with TC-WN-05 for column header visibility
- Phase 13 complete: DailyPlannerView and WeeklyPlannerView are fully replaced by TimelineCanvas
- No regressions in drag, tap-to-edit, week nav, source filter, all-day, unscheduled sections
</success_criteria>

<output>
Create `.planning/phases/13-unified-timelinecanvas/13-04-SUMMARY.md` when done.

Mark Phase 13 plans complete in ROADMAP.md progress table.
</output>
