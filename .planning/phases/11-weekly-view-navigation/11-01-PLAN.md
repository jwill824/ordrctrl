---
phase: 11-weekly-view-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/utils/dateUtils.ts
  - frontend/tests/unit/utils/dateUtils.test.ts
autonomous: true
requirements:
  - WEEK-01
  - WEEK-02

must_haves:
  truths:
    - "formatWeekRange returns 'Jun 1–7' for a same-month week (Jun 1–7)"
    - "formatWeekRange returns 'Jun 30 – Jul 6' for a cross-month week (Jun 30 – Jul 6)"
    - "formatWeekRange returns 'Dec 29 – Jan 4' for a cross-year week boundary"
    - "All unit tests pass: cd frontend && pnpm vitest run dateUtils exits 0"
  artifacts:
    - path: "frontend/src/utils/dateUtils.ts"
      provides: "formatWeekRange export"
      contains: "export function formatWeekRange"
    - path: "frontend/tests/unit/utils/dateUtils.test.ts"
      provides: "Unit tests for formatWeekRange"
      min_lines: 30
  key_links:
    - from: "frontend/tests/unit/utils/dateUtils.test.ts"
      to: "frontend/src/utils/dateUtils.ts"
      via: "import { formatWeekRange } from '@/utils/dateUtils'"
      pattern: "formatWeekRange"
---

<objective>
Add `formatWeekRange(weekStart: Date): string` to dateUtils.ts and write unit tests proving the
same-month, cross-month, and cross-year cases all format correctly.

Purpose: The WeeklyPlannerView nav header (Plan 02) will call this function to render the center
label. It must be implemented and tested before the component consumes it.

Output: One new export in dateUtils.ts; one new test file with ≥5 it() cases passing.
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/src/utils/dateUtils.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add formatWeekRange to dateUtils.ts</name>
  <files>frontend/src/utils/dateUtils.ts</files>
  <behavior>
    - formatWeekRange(new Date(2026, 5, 1)) → "Jun 1–7"   (same month, Mon–Sun)
    - formatWeekRange(new Date(2026, 5, 29)) → "Jun 29 – Jul 5"  (cross-month, no space before en-dash in same-month variant, space around en-dash in cross-month variant)
    - formatWeekRange(new Date(2026, 5, 30)) → "Jun 30 – Jul 6"  (D-05 example, cross-month)
    - formatWeekRange(new Date(2025, 11, 29)) → "Dec 29 – Jan 4"  (cross-year)
  </behavior>
  <action>
    Append the following function to the bottom of `frontend/src/utils/dateUtils.ts` (after the
    existing `formatLocalTime` function). Do NOT modify any existing exports.

    The function computes the Sunday end date using `addDays(weekStart, 6)` (already exported from
    the same file). It formats using the browser's built-in `toLocaleDateString` with
    `{ month: 'short', day: 'numeric' }` — no year is included. The en-dash separator character is
    U+2013 (–), matching D-05 examples exactly.

    Rules:
    - If `start.getMonth() === end.getMonth()`: format as `{MonthAbbr} {startDay}–{endDay}` (no
      spaces around en-dash, e.g., "Jun 1–7")
    - If months differ: format as `{MonthAbbr} {startDay} – {MonthAbbr} {endDay}` (spaces around
      en-dash, e.g., "Jun 30 – Jul 6")

    Use `date.toLocaleDateString('en-US', { month: 'short', day: 'numeric' })` to produce "Jun 1",
    "Jul 6", etc. Then build the string with the en-dash and conditional spacing as above.

    Per D-05: the function anchors to the Monday–Sunday range that `getWeekStart` produces (the
    caller always passes the Monday as `weekStart`). No need to validate — trust the caller.

    Function signature:
    ```
    export function formatWeekRange(weekStart: Date): string
    ```

    Implementation steps:
    1. Compute `end = addDays(weekStart, 6)` (Sunday)
    2. Format start and end using toLocaleDateString('en-US', { month: 'short', day: 'numeric' })
    3. Compare getMonth() values; apply same-month vs cross-month separator logic
    4. Return the formatted string
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run dateUtils 2>&amp;1 | tail -10</automated>
  </verify>
  <done>formatWeekRange is exported from dateUtils.ts and all dateUtils unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write unit tests for formatWeekRange</name>
  <files>frontend/tests/unit/utils/dateUtils.test.ts</files>
  <behavior>
    - Same-month range: weekStart = Jun 1 2026 (Monday) → "Jun 1–7"
    - Cross-month range: weekStart = Jun 29 2026 (Monday) → "Jun 29 – Jul 5"
    - Cross-month D-05 example: weekStart = Jun 30 2026 (Tuesday — not a Monday but test the formatter directly) — use Jun 23 2026 (Monday) → "Jun 23–29"; for Jun 30 test use the formatWeekRange directly with a Date(2026,5,30) to confirm "Jun 30 – Jul 6"
    - Cross-year range: weekStart = Dec 29 2025 (Monday) → "Dec 29 – Jan 4"
    - Single-digit day: weekStart = Jun 2 2026 (Tuesday — pass directly) → start "Jun 2", end "Jun 8" → "Jun 2–8"
  </behavior>
  <action>
    Create the directory `frontend/tests/unit/utils/` if it does not exist (the Write tool will
    create the file; the directory must exist first — use mkdir -p via a pre-step bash command if
    needed, or the tool will create it).

    Create `frontend/tests/unit/utils/dateUtils.test.ts` with the following structure:

    Header imports — follow project convention (explicit imports from vitest, no globals):
    ```
    import { describe, it, expect } from 'vitest';
    import { formatWeekRange } from '@/utils/dateUtils';
    ```

    Write a `describe('formatWeekRange', ...)` block with ≥5 it() cases:

    Case 1 — same month (D-05 first example):
    Input: `new Date(2026, 5, 1)` (June 1, 2026 local midnight — a Monday)
    Expected: `"Jun 1–7"`
    Assertion: `expect(formatWeekRange(new Date(2026, 5, 1))).toBe('Jun 1–7')`

    Case 2 — cross-month (D-05 second example):
    Input: `new Date(2026, 5, 30)` (June 30, 2026 — a Tuesday, but we test the formatter directly)
    Expected: `"Jun 30 – Jul 6"`
    Assertion: `expect(formatWeekRange(new Date(2026, 5, 30))).toBe('Jun 30 – Jul 6')`

    Case 3 — cross-year:
    Input: `new Date(2025, 11, 29)` (December 29, 2025 — a Monday)
    Expected: `"Dec 29 – Jan 4"`
    Assertion: `expect(formatWeekRange(new Date(2025, 11, 29))).toBe('Dec 29 – Jan 4')`

    Case 4 — same month, mid-week start:
    Input: `new Date(2026, 5, 8)` (June 8, 2026 — a Monday)
    Expected: `"Jun 8–14"`
    Assertion: `expect(formatWeekRange(new Date(2026, 5, 8))).toBe('Jun 8–14')`

    Case 5 — cross-month, July into August:
    Input: `new Date(2026, 6, 27)` (July 27, 2026 — a Monday)
    Expected: `"Jul 27 – Aug 2"`
    Assertion: `expect(formatWeekRange(new Date(2026, 6, 27))).toBe('Jul 27 – Aug 2')`

    No mocking needed — pure function.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run dateUtils 2>&amp;1 | grep -E "PASS|pass|✓"</automated>
  </verify>
  <done>frontend/tests/unit/utils/dateUtils.test.ts exists with ≥5 it() cases. All pass: cd frontend && pnpm vitest run dateUtils exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dateUtils inputs | Pure utility — no external input, no I/O, no trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages in this plan — no install surface |
</threat_model>

<verification>
After this plan completes:
- `cd frontend && pnpm vitest run dateUtils` exits 0
- `grep -c 'export function formatWeekRange' frontend/src/utils/dateUtils.ts` returns 1
- `frontend/tests/unit/utils/dateUtils.test.ts` exists with ≥5 it() blocks
</verification>

<success_criteria>
- formatWeekRange exported from dateUtils.ts, handles same-month (no spaces around –) and cross-month (spaces around –)
- 5+ unit tests covering same-month, cross-month, cross-year cases — all pass
- No regressions: cd frontend && pnpm vitest run exits 0 overall
</success_criteria>

<output>
Create `.planning/phases/11-weekly-view-navigation/11-01-SUMMARY.md` when done
</output>
