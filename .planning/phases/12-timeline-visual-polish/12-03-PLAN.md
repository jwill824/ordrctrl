---
phase: 12-timeline-visual-polish
plan: 03
type: execute
wave: 3
depends_on:
  - 12-02
files_modified:
  - frontend/src/services/feed.service.ts
  - frontend/src/components/timeline/PlannerTimeBlock.tsx
  - frontend/src/components/timeline/DraggableTimeBlock.tsx
autonomous: true
requirements:
  - VIS-01
  - VIS-03
  - VIS-04
  - VIS-05
must_haves:
  truths:
    - "Timeline blocks render with a colored left border and a semi-transparent fill matching item.color"
    - "Blocks show an emoji icon prefix before the title when item.icon is non-null"
    - "Blocks have a shadow-sm drop shadow elevating them above the grid"
    - "Daily view blocks animate top/height changes with a 150ms ease transition"
    - "Animation is suppressed during active drag (transition: none)"
    - "Weekly blocks (compact=true) have no transition"
  artifacts:
    - path: "frontend/src/services/feed.service.ts"
      provides: "FeedItem interface with color: string and icon: string | null"
      contains: "color: string"
    - path: "frontend/src/components/timeline/PlannerTimeBlock.tsx"
      provides: "Dynamic color via inline style; shadow-sm; icon prefix; transition when !isDragging && !compact"
      contains: "item.color"
    - path: "frontend/src/components/timeline/DraggableTimeBlock.tsx"
      provides: "Dynamic color in containerStyle; extended transition logic; icon prefix; shadow-sm idle"
      contains: "item.color"
  key_links:
    - from: "frontend/src/components/timeline/PlannerTimeBlock.tsx"
      to: "frontend/src/services/feed.service.ts"
      via: "PlannerItem extends FeedItem (via usePlannerTimeline)"
      pattern: "item\\.color"
    - from: "frontend/src/components/timeline/DraggableTimeBlock.tsx"
      to: "frontend/src/services/feed.service.ts"
      via: "PlannerItem type carries color/icon"
      pattern: "item\\.icon"
---

<objective>
Update the frontend FeedItem type contract and both timeline block components to render with per-task color coding, drop shadow elevation, emoji icon prefix, and smooth CSS animation. This plan implements the visible timeline polish that is the phase's primary user-facing goal.

Purpose: All five VIS requirements manifest in these three files. Block appearance changes here; data contract changes land here first via the FeedItem type extension.
Output: Updated feed.service.ts (FeedItem type), PlannerTimeBlock.tsx, DraggableTimeBlock.tsx.
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/src/services/feed.service.ts
@frontend/src/components/timeline/PlannerTimeBlock.tsx
@frontend/src/components/timeline/DraggableTimeBlock.tsx
@frontend/src/hooks/usePlannerTimeline.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend FeedItem type and update PlannerTimeBlock (color, shadow, icon, animation)</name>
  <files>
    frontend/src/services/feed.service.ts
    frontend/src/components/timeline/PlannerTimeBlock.tsx
  </files>
  <behavior>
    - FeedItem interface has `color: string` and `icon: string | null` (matches backend D-12)
    - PlannerTimeBlock renders `style={{ borderColor: item.color, backgroundColor: item.color + '20' }}` — no more hardcoded `border-blue-500 bg-blue-50 text-blue-900`
    - PlannerTimeBlock wrapper has `shadow-sm` class (VIS-04)
    - Title text uses `text-zinc-900` (not derived from hex) for readability (D-07)
    - When `item.icon` is non-null: `<span className="mr-0.5">{item.icon}</span>` appears before title text (D-09)
    - When `compact=false` and `isDragging=false`: inline style adds `transition: 'top 150ms ease, height 150ms ease'` (D-10)
    - When `compact=true` OR `isDragging=true`: no transition property in style (D-10)
  </behavior>
  <action>
    **frontend/src/services/feed.service.ts:**

    Add two fields to the `FeedItem` interface:
    - `color: string;`
    - `icon: string | null;`

    Place them after the `sourceUrl` field (end of the interface). These are required (non-optional) because the backend always provides a value (default blue for color, null for icon when unset).

    `PlannerItem extends FeedItem` in `usePlannerTimeline.ts` — it inherits color/icon automatically, no change needed there.

    **frontend/src/components/timeline/PlannerTimeBlock.tsx:**

    1. Add `isDragging?: boolean` to the `PlannerTimeBlockProps` interface (default false). This prop is set by DraggableTimeBlock (Task 2).

    2. Replace the `className` prop on the wrapper `<div>`:
       - Remove hardcoded `border-blue-500 bg-blue-50`
       - Keep: `absolute border-l-[3px] rounded-r overflow-hidden`
       - Add: `shadow-sm` (VIS-04)
       - Existing responsive padding classes (`px-1 py-0.5` compact, `px-2 py-1` non-compact) remain

    3. Update the wrapper's `style` prop to merge the existing `{ top, height }` with dynamic color and optional transition:
       ```
       style={{
         top,
         height,
         borderColor: item.color,
         backgroundColor: item.color + '20',
         ...(!compact && !isDragging ? { transition: 'top 150ms ease, height 150ms ease' } : {}),
       }}
       ```
       The spread `...(!compact && !isDragging ...)` applies transition only on daily non-drag renders per D-10.

    4. Change the title `<div>` text color from `text-blue-900` to `text-zinc-900` (per D-07).

    5. Before the title text inside the title `<div>`, add:
       `{item.icon ? <span className="mr-0.5">{item.icon}</span> : null}`

    6. The time-range sub-label already uses `text-zinc-400` — no change needed.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>
    TypeScript compiles with no errors. `FeedItem` has `color: string` and `icon: string | null`. `PlannerTimeBlock` uses `item.color` for borderColor and backgroundColor, has `shadow-sm`, renders icon prefix, and applies 150ms transition on daily non-drag state.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update DraggableTimeBlock (dynamic color, extended transition, icon prefix, shadow-sm idle)</name>
  <files>frontend/src/components/timeline/DraggableTimeBlock.tsx</files>
  <behavior>
    - containerStyle.borderColor = isErrorTint ? '#F87171' : item.color (red-400 hex for error, item color otherwise)
    - containerStyle.backgroundColor = item.color + '20' always (the error tint only changes the border)
    - containerStyle.transition: isDragMove || isDragResize ? 'none' : (isReverting ? revertTransition : 'top 150ms ease, height 150ms ease') — per D-11
    - containerClasses no longer contains 'bg-blue-50', 'border-blue-500', or 'border-red-400' (all color moved to inline style)
    - shadow-sm added to idle state in containerClasses (dragging states already have shadow-md / shadow-sm)
    - Icon prefix rendered before title text (same pattern as PlannerTimeBlock)
    - Title text uses text-zinc-900 not text-blue-900
    - Passes isDragging={isDragMove || isDragResize} to PlannerTimeBlock when used internally (but DraggableTimeBlock does NOT use PlannerTimeBlock; it renders its own markup — apply the same changes directly)
  </behavior>
  <action>
    In `frontend/src/components/timeline/DraggableTimeBlock.tsx`:

    1. Update `containerStyle` (the `React.CSSProperties` object):
       - Add `borderColor: isErrorTint ? '#F87171' : item.color` (red-400 equivalent in hex; avoids needing the Tailwind class)
       - Add `backgroundColor: item.color + '20'`
       - Change `transition` from `isReverting ? revertTransition : 'none'` to:
         `isDragMove || isDragResize ? 'none' : (isReverting ? revertTransition : 'top 150ms ease, height 150ms ease')`
         This implements D-11: no transition during drag, revert animation during revert, smooth motion otherwise.

    2. Update `containerClasses` array:
       - Remove `'bg-blue-50'` from the base string
       - Remove `isErrorTint ? 'border-red-400' : 'border-blue-500'` (color now handled by inline style)
       - Add `'shadow-sm'` to the base string for idle elevation (VIS-04); note drag states already override with `shadow-md`

    3. In the title `<p>` element:
       - Change `text-blue-900` to `text-zinc-900` (per D-07)
       - Before the title text, add: `{item.icon ? <span className="mr-0.5">{item.icon}</span> : null}`

    4. The resize handle knob `bg-blue-300` class can remain as-is (drag affordance chrome, not part of block color requirements).

    Note: DraggableTimeBlock renders its own block markup (it does NOT render PlannerTimeBlock internally). The compact path (`compact=true`) uses DraggableTimeBlock only in WeeklyPlannerView. For weekly compact blocks, the transition is already absent since `compact=true` blocks don't enter the DraggableTimeBlock path for weekly view — but if they do, the transition applies at the container style level. To be safe: DraggableTimeBlock's transition rule uses the drag-state condition only, not compact; the compact weekly blocks never trigger transition because WeeklyPlannerView does not wire onReschedule/onResize.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>
    TypeScript compiles with no errors. DraggableTimeBlock containerStyle has borderColor and backgroundColor using item.color, no hardcoded blue in containerClasses, correct extended transition logic, icon prefix before title, shadow-sm on idle state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → FeedItem type | color and icon values from the server are rendered directly in the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-12-03-01 | Tampering | icon value rendered as text in span | mitigate | React renders text nodes (not innerHTML); emoji strings cannot execute JS. Backend max(10) validation prevents oversized values |
| T-12-03-02 | Tampering | color value in inline style | mitigate | CSS `borderColor` and `backgroundColor` accept hex strings; non-hex values render as invalid (no color) not as script; backend regex validation enforces valid hex |
| T-12-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
1. `cd frontend && npx tsc --noEmit` exits 0
2. `grep "color: string" frontend/src/services/feed.service.ts` matches FeedItem field (not optional)
3. `grep "item\.color" frontend/src/components/timeline/PlannerTimeBlock.tsx` shows inline style usage
4. `grep "item\.color" frontend/src/components/timeline/DraggableTimeBlock.tsx` shows inline style usage
5. `grep "shadow-sm" frontend/src/components/timeline/PlannerTimeBlock.tsx` confirms elevation class
6. `grep "150ms ease" frontend/src/components/timeline/DraggableTimeBlock.tsx` confirms transition string
</verification>

<success_criteria>
- FeedItem.color and FeedItem.icon are typed as required fields (string, string|null) matching backend output (per D-12)
- PlannerTimeBlock uses inline style for borderColor and backgroundColor from item.color (per D-07)
- Both block components show icon prefix when item.icon is non-null (per D-09 / VIS-03)
- Both block components have shadow-sm for elevation (per D-08 / VIS-04)
- Daily PlannerTimeBlock applies 150ms ease transition on top/height when not dragging and not compact (per D-10 / VIS-05)
- DraggableTimeBlock has extended transition logic: none during drag, revert during revert, 150ms ease otherwise (per D-11 / VIS-05)
- Title text is text-zinc-900 in both components (per D-07)
- TypeScript compiles with no errors
</success_criteria>

<output>
Create `.planning/phases/12-timeline-visual-polish/12-03-SUMMARY.md` when done
</output>
