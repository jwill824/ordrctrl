# Physical device testing overrides — copy to .env.device.local and fill in your values
# Loaded on top of .env when running: pnpm dev:device (vite --mode device)
# See quickstart.md for full setup instructions

VITE_API_URL=https://your-domain.ngrok-free.app
