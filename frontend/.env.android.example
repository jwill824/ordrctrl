# Android Emulator overrides — copy to .env.android.local
# Android emulator uses 10.0.2.2 as the alias for the host Mac's localhost.
VITE_API_URL=http://10.0.2.2:4000
VITE_APP_URL=http://10.0.2.2:3000
